# StarNode 生产发布检查清单（Release Checklist）

> 目标：确保每次发布都可验证、可回滚、可追责。

## 1. 代码质量门禁（必须全绿）
- [ ] `npm run lint` 通过
- [ ] `npm run test` 通过
- [ ] `npm run typecheck` 通过（Node 22.x）
- [ ] `npm run build` 通过
- [ ] CI Workflow `CI Quality Gate` 通过

## 2. 功能一致性回归（MVP 主链）
- [ ] 创建笔记 -> 星球统计变化正常
- [ ] 编辑笔记 -> 关联与筛选结果同步更新
- [ ] 冰封/解冻 -> 视图模式与统计口径一致
- [ ] 批量迁移/删除 -> 提示数量与实际变更一致
- [ ] 撤销一步（Undo）-> 完整恢复快照

## 3. 数据与兼容性
- [ ] 本地存储读取与迁移测试通过（v1/v2 -> v3）
- [ ] `deleted/frozen` 字段逻辑无回归
- [ ] 新增字段具备默认值与向后兼容策略

## 4. 性能基线（1000 笔记场景）
- [ ] 筛选操作 P95 < 100ms
- [ ] 批量操作 P95 < 150ms
- [ ] 首次可交互时间无显著退化（与基线对比）

## 5. 运行与回滚
- [ ] 发布记录包含 commit/tag 与变更摘要
- [ ] 可在 10 分钟内回滚到上一个稳定版本
- [ ] 回滚后数据读写正常，无 schema 不兼容

## 6. 发布后 24h 观察
- [ ] 前端运行错误率（如页面崩溃）无异常抬升
- [ ] 用户关键路径（写入、筛选、批量）无高频失败
- [ ] 收到高优先级问题时有明确 owner 与处理时限
