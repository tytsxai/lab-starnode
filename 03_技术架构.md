# StarNode 技术架构 v2

## 1. 架构目标
- **低延迟写作**：输入即时可见，不因 3D 渲染阻塞。
- **本地优先**：离线可用，云同步为增强能力。
- **可演进**：MVP 与未来商业版可平滑迁移。

## 2. 总体架构
```txt
Editor(UI) -> App Store(Zustand) -> Derived World State -> Renderer(R3F)
                          |
                          +-> Local Storage(JSON/SQLite)
                          +-> Link Engine(keyword/tag)
```

## 3. 模块分层
### 3.1 apps/web
- Next.js 前端壳层
- 路由与页面组合
- 用户交互入口

### 3.2 packages/core
- 领域模型：Note / Planet / Link
- 纯函数计算：质量、阶段、颜色
- 规则引擎（不依赖 UI）

### 3.3 packages/renderer
- R3F 渲染组件
- 星球网格、轨道线、相机控制
- LOD 策略（后续扩展）

### 3.4 packages/storage
- 本地存储接口与实现
- JSON 方案（MVP）
- SQLite 方案（后续）

## 4. 核心数据模型（MVP）
```ts
Note {
  id: string
  title: string
  content: string
  tags: string[]
  planetId: string
  updatedAt: string
}

Planet {
  id: string
  name: string
  theme: string
  noteCount: number
  mass: number
  stage: 'asteroid' | 'dwarf' | 'planet' | 'giant'
}
```

## 5. 关键技术决策（ADR 摘要）
1. **状态管理选 Zustand**：轻量、渲染 loop 友好。
2. **首版存储选 JSON**：开发速度快，便于调试。
3. **3D 选 R3F**：与 React 心智统一，工程协作成本低。
4. **关联先规则后 AI**：先稳定体验，再上成本模块。

## 6. 性能策略
- 远景星球简化材质
- 仅对当前选中星球渲染高细节
- 关联线按视图窗口裁剪
- 计算密集逻辑迁移到 worker（P1）

## 7. 演进路线
- v0（当前）：本地单机、规则关联
- v1：云同步、向量检索、AI 助手
- v2：多端协同、市场生态
