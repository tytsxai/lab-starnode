# StarNode 开发路线图（执行版）

## Phase 0（本周）：搭骨架
- [x] 文档完善（愿景/PRD/架构/商业）
- [x] 初始化 monorepo 目录
- [x] Web 应用可启动
- [x] 首个 3D 星球可渲染

## Phase 1（第 1-2 周）：MVP 闭环
- [x] 笔记 CRUD + 自动保存
- [x] 分类映射星球
- [x] 宇宙视图交互（缩放/选中/跳转）
- [x] 星球成长规则（noteCount -> stage）
- [x] Demo 数据与录屏

**交付定义（DoD）**
- 可完成“创建笔记 -> 星球变化 -> 进入回顾”的完整流程
- 本地数据重启不丢失

## Phase 2（第 3-4 周）：产品可用性
- [x] 冰封/解冻机制（支持单条与批量操作，默认活跃视图）
- [x] 基础关键词关联线（标签 + 关键词混合评分）
- [x] 检索与过滤（搜索/排序/标签/可见性/关联模式）
- [ ] 性能优化（1000 笔记压测 + memo/索引策略）
- [ ] 工程护栏（CI: Node 22 + lint + test + typecheck + build，workflow 权限待开通后推送）

## Phase 3（第 5-8 周）：Beta 准备
- [ ] AI 标签与摘要
- [ ] 公开星球（只读）
- [ ] 错误监控与指标埋点
- [ ] 小范围用户测试

## 下一阶段最小清单（Phase 2 收口）
1. [ ] 完成 1000 笔记压测脚本与基线报告（渲染帧率、筛选延迟、批量操作耗时）
2. [ ] 对关键词计算加 memo/index（避免输入中重复全量计算）
3. [ ] 将 `includeFrozen` 接入“历史回放视图”开关（UI 明示统计口径）
4. [x] 增补 storage 迁移测试（v1→v2→v3 回归）
5. [ ] main 分支开启保护（必须 PR + 必须 CI 通过）

## 今日开发清单（可直接执行）
1. [x] 安装依赖并跑通 `apps/web`
2. [x] 实现 `PlanetCanvas`（静态球体 + 示例数据）
3. [x] 实现 `NoteStore`（内存 + 本地持久化）
4. [x] 打通页面：左编辑区 + 右宇宙区
5. [x] 新增“当前星球笔记删除”闭环
6. [x] 新增标签输入与跨星球可解释关联线
7. [x] 实现 3D 星球点击选中联动
8. [x] 星球配置统一收敛到 core（消除前端硬编码）
9. [x] 新增关联线筛选开关（全部/仅当前星球）
10. [x] 增加编辑快捷键（Ctrl/Cmd+Enter）
11. [x] 拆分 LinkPanel（降低 UniversePanel 复杂度）
12. [x] 空态星球可见（无笔记时仍可渲染基础星球）
13. [x] 增加 Node LTS（22）版本门禁脚本，避免非 LTS 下 TypeScript 崩溃
14. [x] 打通笔记编辑链路（进入编辑/保存更新/取消编辑）
15. [x] 关联面板支持双向跳转（左侧星球/右侧星球）
16. [x] 新增编辑表单校验（空值/标题长度/标签数量）
17. [x] 当前星球笔记支持搜索与排序（更新时间/标题）
18. [x] 当前星球笔记支持标签 chips 过滤
19. [x] 支持批量操作（批量迁移 / 批量删除）
20. [x] 批量删除增加二次确认（防误删）
21. [x] 新增一次性撤销（Undo）用于回滚最近批量操作
22. [x] 支持“全选当前过滤结果 / 清空选择 / 重置过滤”快捷操作
23. [x] 新增操作反馈 Toast（迁移/删除/撤销）
24. [x] 新增笔记冰封/解冻机制（单条/批量 + 视图切换：活跃/全部/冰封）
25. [x] 新增关键词提取与混合关联评分（标签证据 + 关键词证据 + 分数拆解）
26. [x] 关联面板新增模式筛选（仅标签/仅关键词/混合）与证据词回填搜索
27. [x] 抽离 `noteForm` / `batchHelpers` 纯函数并新增 web/store 单测
28. [x] storage 新增 schemaVersion 迁移与节流写入
29. [ ] 新增 GitHub Actions CI（Node 22 / lint / test / typecheck / build，已本地完成待推送）
